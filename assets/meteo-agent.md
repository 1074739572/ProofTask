# Meteo Agent · 风眼

极简动画标志：开放气流螺旋代表风场/等压线和 Agent 循环；沿轨道移动的小点代表观测；中心呼吸点代表推理节奏；底部小箭头很轻地呼应“南”。

`meteo-agent.svg` 适合浅色背景，`meteo-agent-dark.svg` 适合终端深色背景。两者都是纯 SVG + CSS 动画，支持 `prefers-reduced-motion`；没有直接复制学校或 DeepSeek 的商标。
