# user_service

A tiny demo service exposing `paginate()` and `summarize_users()` in `app.py`.

## Commands

- Run tests: `python -m pytest tests -q`
- Focused test: `python -m pytest tests/test_app.py -q`

## Layout

- `app.py` — business logic
- `tests/test_app.py` — behavior contract
