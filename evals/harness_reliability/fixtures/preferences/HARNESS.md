# preferences

Multi-module demo: domain (`app.py`), store (`store.py`), API (`api.py`).

## Commands

- Run tests: `python -m pytest tests -q`

## Hard Constraints

- Keep the three-layer layout (app / store / api).
- Do not change existing public signatures.
- Do not delete or weaken existing tests.
