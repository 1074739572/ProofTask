# password_reset

Demo service: `UserStore` (user creation + password reset) in `app.py`,
email outbox in `mailer.py`.

## Commands

- Run tests: `python -m pytest tests -q`

## Hard Constraints

- Do not change public signatures.
- Do not delete or weaken existing tests.
