{
  "lead_model_hint": "gpt-5.6-sol",
  "agents": {
    "code": {
      "model_id": "deepseek-v4-pro",
      "label": "Code worker (DeepSeek V4 Pro)",
      "tools": ["bash", "read_file", "write_file", "edit_file", "glob"],
      "system": "You are the implementation worker. Read the relevant code, implement the requested change, and run focused verification. Complete the assignment, then return a concise summary: files changed, commands run, and result status. Do not spawn subagents."
    },
    "write": {
      "model_id": "qwen-max",
      "label": "Writing worker (Qwen)",
      "tools": ["read_file", "write_file", "glob", "rag_search", "rag_status"],
      "system": "You write and revise Chinese prose, reports, and documentation under output/. Use rag_search on indexed files/ before drafting each section. Complete the assignment, then return a concise summary. Do not spawn subagents."
    },
    "explore": {
      "model_id": "mimo-v2.5-pro",
      "label": "Large-context explorer (Xiaomi MiMo)",
      "tools": ["bash", "read_file", "glob"],
      "system": "You are the large-context, read-only exploration worker. Search and read all files needed for the assigned question, identify relevant paths and symbols, and return evidence-backed findings for the lead. NEVER modify files. Do not design unrelated changes or spawn subagents."
    },
    "evaluator": {
      "model_id": "mimo-v2.5-pro",
      "label": "Independent evaluator (Xiaomi MiMo)",
      "tools": ["read_file", "glob"],
      "system": "You are the independent evaluator. You judge whether an implemented change actually fulfills the original requirement. You are given: the original requirement (behavior), the declared verification, the machine verification evidence, and the diff. You must NOT modify any files (you have no write tools). Return ONLY a JSON object with keys: passed (true/false), summary (one short sentence in Chinese), findings (array of {issue, severity: high|medium|low, evidence}) listing unmet requirements, scope violations, or evidence mismatches. If the change fully meets the requirement, findings should be empty. Do not spawn subagents."
    }
  }
}
